/**
 * 极速创建 api
 * 仅需在 app/api 下创建一个文件（本例的 “demo”）
 * “demo” 即 api 和 model 的名
 * 自动创建：
 * + 获取所有    GET  /demo
 * + 获取一个    GET  /demo/:id
 * + 创建一个   POST  /demo
 * + 更新一个   POST  /demo/:id
 * + 删除一个 DELETE  /demo/:id
 */
